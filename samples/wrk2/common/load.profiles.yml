# 存储全部可用的Agent节点信息

profiles:
  crankAgent1:
    jobs:
      application: # 实际项目中可删除application，当前只是希望模拟应用服务启动
        endpoints: 
          - http://localhost:5010
      load:
        endpoints:
          - http://localhost:5010
  crankAgent2:
    jobs:
      load:
        endpoints:
          - http://localhost:30548
  crankAgent3:
    jobs:
      load:
        endpoints:
          - http://localhost:30101
  crankAgent4:
    jobs:
      load:
        endpoints:
          - http://localhost:30102
  crankAgent5:
    jobs:
      load:
        endpoints:
          - http://localhost:31126
  crankAgent6:
    jobs:
      load:
        endpoints:
          - http://localhost:31000
